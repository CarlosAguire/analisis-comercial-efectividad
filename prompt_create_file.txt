# Datos de entrada

Se recibe una variable llamada `JSON_TABLE` (el valor está delimitado entre tres comillas). `JSON_TABLE` es una cadena de texto que contiene un JSON válido que representa una lista de objetos, donde cada objeto corresponde a una fila de una tabla.

JSON_TABLE
"""
JSON_TABLE
"""

---

# Rol

Eres un especialista en procesamiento de datos con dominio en Python y generación de archivos Excel estructurados.

---

# Tarea

Recibir la variable `JSON_TABLE`, parsearla y generar un archivo Excel llamado **`datos-efectividad-comercial-IA.xlsx`** que contenga todos los datos en una tabla de Excel con el nombre **`Datos`**.

---

# Instrucciones

Usa el intérprete de código Python para ejecutar los siguientes pasos:

1. Parsear `JSON_TABLE` usando `json.loads()` para obtener la lista de diccionarios.
2. Tratar cualquier valor nulo, vacío o no-texto como cadena vacía (`""`). No generar errores ni detener el flujo.
3. Convertir la lista de diccionarios en un `DataFrame` de pandas, respetando exactamente los nombres de columnas presentes en el JSON sin modificarlos.
4. Crear el archivo Excel `datos-efectividad-comercial-IA.xlsx` usando `openpyxl` como motor.
5. Escribir los datos en una hoja de Excel y convertir el rango de datos en una **tabla de Excel con el nombre `Datos`** usando `openpyxl.worksheet.table.Table`.
6. Guardar y cerrar el archivo correctamente.
7. Poner el archivo a disposición para descarga.

**No incluir texto, explicaciones ni comentarios fuera de la ejecución del código y el archivo generado.**